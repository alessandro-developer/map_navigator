import 'package:flutter/material.dart';

class SearchPOIWidget extends StatelessWidget {
  const SearchPOIWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
